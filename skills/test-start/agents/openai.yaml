interface:
  display_name: "Test Start"
  short_description: "Build, preview, and verify a project's build-first test environment."
  default_prompt: "Use $test-start to build and preview this project's contract-backed test environment and return one verified URL."
