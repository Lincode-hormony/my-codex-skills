display_name: Test Init
short_description: Integrate a persistent build-plus-preview test-entry base into a client-rendered web app.
default_prompt: Integrate a persistent build-plus-preview test-entry base into this project so downstream skills can launch stable screens, inject test state, and wait for contract-defined ready states reliably.
