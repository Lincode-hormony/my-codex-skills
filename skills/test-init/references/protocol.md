# Test-Entry Protocol

This file is retained as a compatibility pointer.

The canonical build-first protocol now lives in [build-first-protocol.md](build-first-protocol.md).

Use that file for:

- launch contract rules
- bridge requirements
- screen metadata
- ready contract expectations
- downstream expectations
