# Integration Patterns

This file is retained as a compatibility pointer.

The canonical build-first integration guidance now lives in [build-first-integration-patterns.md](build-first-integration-patterns.md).

Use that file to choose:

- the least invasive build-first attachment point
- the command model to record in the summary
- how to expose the public bridge
- how to keep the contract stable during incremental extension
